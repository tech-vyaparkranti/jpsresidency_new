<?php
/**
 * Adds a wide-range of theme supports for WP Dark Mode
 *
 * @package WP Dark Mode
 * @since 5.0.0
 */

// Namespace.
namespace WP_Dark_Mode;

// Exit if accessed directly.
defined( 'ABSPATH' ) || exit( 1 );

if ( ! class_exists( __NAMESPACE__ . 'Theme_Supports' ) ) {
	/**
	 * Adds a wide-range of theme supports for WP Dark Mode
	 *
	 * @package WP Dark Mode
	 * @since 5.0.0
	 */
	class Theme_Supports extends Base {

	}
}
